{
  "name": "alfie-translations-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "create-db": "wrangler d1 create alfie-translations",
    "init-schema": "wrangler d1 execute alfie-translations --file=./schema.sql",
    "init-schema-local": "wrangler d1 execute alfie-translations --local --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251225.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.28.0"
  }
}
